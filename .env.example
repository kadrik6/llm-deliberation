# Required for all runs
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Required only when red-team is enabled
GEMINI_API_KEY=

# Default profile: balanced | max | economy
LLM_PROFILE=balanced

# The third model can be globally enabled/disabled here.
# CLI --red-team / --no-red-team overrides this.
RED_TEAM_ENABLED=true

# Optional per-provider overrides.
# Leave blank to use the selected profile.
OPENAI_MODEL=
ANTHROPIC_MODEL=
GEMINI_MODEL=

# Reasoning / thinking levels
OPENAI_EFFORT=high
ANTHROPIC_EFFORT=high
GEMINI_THINKING_LEVEL=high

# Maximum visible output tokens per call.
MAX_OUTPUT_TOKENS=5000
